<nav class="navbar">
    <div class="navbar-container">
        <a href="{{ url_for('home') }}" class="logo">Course Hub</a>
        <ul class="navbar-links">
            <li><a href="{{ url_for('programmes-list') }}">Programs</a></li>
            <li><a href="{{ url_for('register-form') }}">Register</a></li>
            <li><a href="{{ url_for('contact-form') }}">Contact</a></li>
        </ul>
        <div class="navbar-login">
            <a href="{{ url_for('login') }}">Login</a>
        </div>
    </div>
</nav>
